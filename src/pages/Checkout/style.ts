import styled from "styled-components"

export const Container = styled.table`
  width: 100%;
  text-align: left;
  gap: 10px;
  color: #ffffff;
  font-size: 1.15rem;
  .subtotal {
    font-size: 1.2rem;
  }
`
